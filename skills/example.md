# Example Skill

This is a sample skill file. Place `.md` files in the `skills/` directory
to inject custom instructions and knowledge into the agent's system prompt.

## Usage

- Skills are loaded automatically on startup.
- Run `:ingest-skills` to reload without restarting.
- Each `.md` file becomes a "Skill: filename" section in the system prompt.
